include: package:flutter_lints/flutter.yaml

linter:
  rules:
    - prefer_const_constructors
    - prefer_final_locals
    - avoid_print
    - secure_pubspec_urls
    - unnecessary_statements

analyzer:
  errors:
    todo: ignore
